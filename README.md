# mail




# Run Script
```
python app.py -h
```
shows
```
usage: app.py [-h] [--populate-db] [--rule-description RULE_DESCRIPTION]

Apple mail app

options:
  -h, --help            show this help message and exit

Required arguments:
  --populate-db         Populate the database
  --rule-description RULE_DESCRIPTION
                        Description of the rule
 